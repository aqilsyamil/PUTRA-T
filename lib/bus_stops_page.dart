// bus_routes_page.dart

import 'package:flutter/material.dart';

class BusStopsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('Bus Stops'),
    );
  }
}